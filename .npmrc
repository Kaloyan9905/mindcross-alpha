# Disable pnpm 11 interactive build-script approval (CI/Docker friendly).
# Native deps (sharp, esbuild) are not exercised by this minimal skeleton.
# Re-evaluate when Image-component usage or other native deps become critical.
ignore-build-scripts=true
