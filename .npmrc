# Build scripts for native deps are allow-listed via `pnpm.onlyBuiltDependencies`
# in package.json (esbuild, sharp, unrs-resolver, @node-rs/argon2). pnpm 10+
# refuses to silently ignore build scripts, so this is the supported approach.
